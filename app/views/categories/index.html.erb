<h1>Advice Board</h1>
<h2>Categories</h2>
<% @categories.each do |category| %>
  <%= link_to category.name, category_path(category) %><br>
<% end %>

<%= button_to "Add a new piece of advice", new_advice_path, method: 'get' %><br>
<%= link_to "View All", advices_path %>

